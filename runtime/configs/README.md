# runtime/configs — SSOT (lecture seule)

- **ssot/** — ROUTE_CATALOG.json, ENTITLEMENTS_CATALOG.json, FEATURE_FLAGS.json, etc.
- **environments/** — dev/stage/prod JSON (si utilisé)
- **env/** — configs env (alias ou legacy)

Aucun code métier ; pas d’import depuis runtime/reports.
