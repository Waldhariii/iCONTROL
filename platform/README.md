# platform — Shared platform (API, libs transverses)

- **api/** — backend API (src/index.ts, middleware/, …)
- **libs/** — libs partagées (observability server, auth, utils) — si utilisé
- **db/** — migrations/, seeds/
- **workers/** — jobs/, queues/
