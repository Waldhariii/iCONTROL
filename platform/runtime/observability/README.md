# Platform observability

Unified event stream: `runtime/reports/platform_events.jsonl`

**Logged:** release operations, freezes, breakglass, tenant provisioning, workflow executions, adapter calls, overrides usage.

Feeds future AI Ops.
