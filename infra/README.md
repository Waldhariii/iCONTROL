# infra — Infrastructure-as-code / déploiement

- **terraform/** — envs, modules (si utilisé)
- **k8s/** — manifests helm/kustomize (si utilisé)
- **docker/** — images, compose, registry policies
- **secrets/** — templates uniquement (jamais secrets réels)
