# E2.1 Tenant Isolation Hard Mode

Scaffold:
- core-kernel/src/tenants/tenantContext.ts

Next:
- thread tenantId through command bus + event bus
- enforce in writeGovernor + vfs-write
- add contract tests + gate
