# E2.3 Policy Engine

Scaffold:
- core-kernel/src/policy/policyEngine.ts

Next:
- integrate into writePolicy / writeGovernor
- create policy registry per module
- add contract tests + audit reason codes
