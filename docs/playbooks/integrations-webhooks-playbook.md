# Integrations Webhooks Playbook

1. Define connector + version in SSOT.
2. Add tenant connector config (refs only).
3. Add webhook definition (inbound/outbound).
4. Add event subscription (outbound).
5. Compile + gates + publish.
6. Validate signature (inbound) or masking + retries (outbound).
