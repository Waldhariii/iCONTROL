# P1.7 — Cache Audit Policy Hardening (redactedSnapshot + stable keys)

## Scope
- Policy: add `redactedSnapshot()` for cache audit (JSON-safe) and ensure stable keys for UI consumption.
- System UI: prefer `rt.__cacheAudit.redactedSnapshot()` when present; fallback to `snapshot()`.

## Evidence
- Gate: `./scripts/vitest-app.sh` PASS

## Rollback
See `RUNBOOK_ROLLBACK.md`
