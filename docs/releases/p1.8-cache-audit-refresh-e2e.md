# P1.8 — Cache Audit Refresh E2E (OBS + UI Contract)

## Scope
- System page: Cache Audit Refresh emits OBS event (best-effort, no-throw).
- UI contract: validates Refresh button emits `AUDIT_CACHE_REFRESH`.

## Evidence
- Gate: `./scripts/vitest-app.sh` PASS

## Rollback
See `RUNBOOK_ROLLBACK.md`
