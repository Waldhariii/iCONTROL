# Governance Expansion — Canonical Enforcement

Rules now BLOCKING:

- No parallel roots allowed.
- Generated folders (_artifacts, _audit) must never be tracked.
- Kernel boundaries enforced via gates.
- Modules cannot import app/server.

This governance layer is considered **Tier-0 critical**.
