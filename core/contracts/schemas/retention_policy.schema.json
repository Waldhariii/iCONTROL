{
  "$id": "retention_policy.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["policy_id", "scope", "target_model_id", "retain_days", "purge_strategy", "legal_hold", "hold_reason"],
  "properties": {
    "policy_id": { "type": "string", "minLength": 1 },
    "scope": { "type": "string", "minLength": 1 },
    "target_model_id": { "type": "string", "minLength": 1 },
    "retain_days": { "type": "number", "minimum": 0 },
    "purge_strategy": { "type": "string", "enum": ["delete", "anonymize", "archive"] },
    "legal_hold": { "type": "boolean" },
    "hold_reason": { "type": "string" }
  }
}
