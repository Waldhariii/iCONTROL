{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "secret_vault_ref.v1",
  "title": "secret_vault_ref",
  "type": "object",
  "required": ["ref_id", "tenant_id", "kind", "provider", "pointer"],
  "additionalProperties": false,
  "properties": {
    "ref_id": {"type": "string"},
    "tenant_id": {"type": "string"},
    "kind": {"type": "string", "enum": ["api_key", "oauth_token", "hmac_secret"]},
    "provider": {"type": "string", "enum": ["local_env", "file", "kms_future"]},
    "pointer": {"type": "string"}
  }
}
