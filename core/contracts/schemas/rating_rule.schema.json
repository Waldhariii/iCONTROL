{
  "$id": "rating_rule.v1",
  "title": "rating_rule",
  "type": "object",
  "required": ["rule_id", "meter_id", "unit_price", "currency"],
  "properties": {
    "rule_id": { "type": "string" },
    "meter_id": { "type": "string" },
    "unit_price": { "type": "number", "minimum": 0 },
    "currency": { "type": "string" },
    "cap": { "type": "number", "minimum": 0 },
    "tier_pricing": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["up_to", "unit_price"],
        "properties": {
          "up_to": { "type": "number", "minimum": 0 },
          "unit_price": { "type": "number", "minimum": 0 }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
