# Dependencies / builds
node_modules/
dist/
.DS_Store

# Local env
*.local
.env
.env.*

# Reports: keep the folder, ignore contents except index
_REPORTS/*
!_REPORTS/.gitkeep
!_REPORTS/INDEX.md

# --- Reports hygiene (keep INDEX.md tracked, ignore volatile artifacts) ---
_REPORTS/_TMP_*.md
_REPORTS/ICONTROL_AUDIT_*.md
_REPORTS/CORE_KERNEL_FREEZE_REPORT_*.md
_REPORTS/_BK_*/
_REPORTS/_BACKUPS_*/
_INTAKE/
iCONTROL/
app/dist/

# local reports (generated)
_REPORTS/
ICONTROL_SYSTEM_*.md

# iCONTROL local artifacts
**/.DS_Store
**/_REPORTS/
app/node_modules/
**/node_modules/
**/dist/
_REPORTS/**

# =========================
# ICONTROL_GIT_HYGIENE_V1
# =========================
.DS_Store
**/.DS_Store

# Local build artifacts
dist/
app/dist/

# Tool caches
node_modules/
.vite/
.cache/

# Local reports (keep reports out of git by default)
_REPORTS/
**/_REPORTS/
*.md.report
ICONTROL_*_REPORT_*.md
ICONTROL_*_ANALYSIS_*.md
ICONTROL_*_PROGRESS_*.md
_RELEASES/
**/.vite/
# Runtime data (never commit)
_DATA/
app/_DATA/

# ReleaseOps local backups (do not version)
scripts/release/publish.zsh.bak.*

# Local safety backups (generated during patching)
app/src/policies/cache.registry.ts.BAK_*
app/src/policies/cache.registry.ts.BAK_P1_*
app/src/policies/*.BAK_*

# Tauri desktop build artifacts
app-desktop-*/dist/
app-desktop-*/src-tauri/target/
app-desktop-*/src-tauri/gen/

# --- Security / Build artifacts (must not be tracked) ---
dist/
**/dist/
build/
**/build/
target/
**/target/
.vite/
**/.vite/
.cache/
**/.cache/

# --- Local bundles / packs / audits (ephemeral) ---
_pr_pack/
_pr_pack_final/
_gov_pack/
_gov_pack_final/
_audit_system_innovex_clean/
_audit_*/
_security_inventory_*/
_report_*/
_reports_*/
_wt-*/
_wt-*/

# --- WIP / backups (must never be committed) ---
._WIP_BACKUPS/
**/._WIP_BACKUPS/

# --- macOS noise ---
.DS_Store
._*

# --- SECURITY / HYGIENE ---
# Build artifacts must never be tracked
dist/
**/dist/
build/
**/build/
target/
**/target/

# Local WIP backups / inventories must never be tracked
._WIP_BACKUPS/
_security_inventory_*/
_audit_system_innovex_clean/
_pr_pack*/
_gov_pack*/

