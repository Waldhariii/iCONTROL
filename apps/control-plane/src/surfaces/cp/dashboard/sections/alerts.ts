// Section Alertes - Alertes & Incidents
export function renderAlertsView(container: HTMLElement): void {
  container.innerHTML = `
    <div style="padding: 20px;">
      <h2 style="color: var(--text-primary); margin-bottom: 16px;">Alertes & Incidents</h2>
      <p style="color: var(--text-muted);">Liste des alertes + incidents à implémenter</p>
    </div>
  `;
}
